<!-- This has been generated by OpenAI and then adapted -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spotify access token</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f0f0f0;
        }
        .container {
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            margin-bottom: 20px;
            color: #333;
        }
        .copy-btn {
            padding: 10px 20px;
            font-size: 16px;
            color: #fff;
            background-color: #28a745;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .copy-btn:hover {
            background-color: #218838;
        }
        .hidden-text {
            display: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Authentication successful</h1>
        <h2>Click below to copy the token</h2>
        <input type="text" id="tokenText" class="hidden-text" value="<%= token %>">
        <button class="copy-btn" onclick="copyText()">Copy token</button>
    </div>

    <script>
        function copyText() {
            const tokenText = document.getElementById('tokenText');
            tokenText.select();
            tokenText.setSelectionRange(0, 99999);  // For mobile
            navigator.clipboard.writeText(tokenText.value);
        }
    </script>
</body>
</html>